{
  "name": "inkedmayhem",
  "version": "1.0.0",
  "description": "InkedMayhem - Independent creator platform",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'",
    "lint": "node --check public/js/app.js && node --check public/sw.js"
    "lint": "node --check public/js/app.js"
    "lint": "node -e \"console.log('Lint check: no ESLint config present; static checks only.')\""
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0",
    "@netlify/functions": "^2.0.0",
    "bcryptjs": "^2.4.3",
    "esbuild": "^0.24.0",
    "jsonwebtoken": "^9.0.0",
    "sharp": "^0.34.5",
    "stripe": "^14.0.0",
    "undici": "^7.21.0"
  }
}
