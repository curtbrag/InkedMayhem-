[build]
  publish = "public"
  functions = "netlify/functions"

# Scheduled function: auto-publish content on schedule (every 15 min)
[functions."scheduled-publish"]
  schedule = "*/15 * * * *"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
